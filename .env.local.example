# Environment variables for integration tests
# Copy this file to .env.local and fill in your values

# Base Sepolia RPC URL (or use a private RPC provider)
RPC_URL=https://sepolia.base.org

# Private key for test wallet (must have Base Sepolia ETH and testnet USDC)
# IMPORTANT: Never commit this file with real private keys!
PRIVATE_KEY=0x...

# Perp Manager contract address on Base Sepolia
PERP_MANAGER_ADDRESS=0x...

# USDC contract address on Base Sepolia
USDC_ADDRESS=0x...

# Optional: Specific perp ID to test with (if you have one already created)
TEST_PERP_ID=

# Optional: Chain ID (defaults to 84532 for Base Sepolia)
CHAIN_ID=84532
